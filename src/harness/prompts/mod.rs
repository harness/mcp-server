//! Prompt management for the Harness MCP server

use crate::types::Result;

/// Register prompts with the MCP server
pub fn register_prompts(_server: &mut crate::harness::HarnessMcpServer) -> Result<()> {
    // TODO: Implement prompt registration
    Ok(())
}