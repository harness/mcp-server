# Harness MCP Server Configuration

# Required: Harness API Key (account ID will be extracted automatically)
HARNESS_API_KEY=pat.your_account_id.your_token_id.your_token_value

# Optional: Default organization and project IDs
HARNESS_DEFAULT_ORG_ID=your_org_id
HARNESS_DEFAULT_PROJECT_ID=your_project_id

# Optional: Harness base URL (defaults to https://app.harness.io)
HARNESS_BASE_URL=https://app.harness.io

# Optional: Toolsets to enable (comma-separated, defaults to "default")
HARNESS_TOOLSETS=default,pipelines,connectors,dashboards

# Optional: Modules to enable (comma-separated)
HARNESS_ENABLE_MODULES=CORE,CI,CD,CCM

# Optional: Enable license validation
HARNESS_ENABLE_LICENSE=false

# Optional: Read-only mode
HARNESS_READ_ONLY=false

# Optional: Logging configuration
HARNESS_LOG_FILE=/var/log/harness-mcp-server.log
HARNESS_DEBUG=false

# Optional: Output directory for files
HARNESS_OUTPUT_DIR=/tmp/harness-mcp-output

# Internal mode only: Bearer token for authentication
HARNESS_BEARER_TOKEN=your_bearer_token